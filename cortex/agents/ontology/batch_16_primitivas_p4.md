# MATRIZ 1: PRIMITIVAS DE COLAPSO (061 - 082)

| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
|----|-----------|------------------|----------------------|------------------|-----------------|----------|--------------|
| PRM-061 | Tormenta de File Descriptors | Se abren miles de archivos/sockets sin cerrar. | Scraping o Búsqueda Masiva | `Too many open files (EMFILE)` | min | Fatal | Restricción estricta de Context Managers (`with`) |
| PRM-062 | Fragmentación del Heap SQLite | Inserciones y borrados masivos inflan el archivo de DB pero SQLite no lo reduce. | Rotación de Memoria | Tamaño del archivo DB crece sin límite | días | Moderada | Ejecución programada de `VACUUM` |
| PRM-063 | Bucle de Recursión YAML | Un string YAML tiene una referencia anidada circular (`&id001 -> *id001`). | Parser YAML inseguro (`yaml.load`) | `RecursionError` o parálisis CPU | ms | Fatal | Forzar uso estricto de `yaml.safe_load` |
| PRM-064 | Inconsistencia de Indentación (Tabs/Spaces) | Un script mezcla Tabs y Espacios y Python se niega a ejecutar. | Generación de código LLM sin restricción de formato | `TabError` en runtime | ms | Grave | Configurar el linter (`ruff`) para autoborrar Tabs (Fallo Rápido) |
| PRM-065 | Exhaustión por Regex Catastrófico | Un patrón regex complejo (ej. `(a+)+$`) procesa una cadena larga forzando Backtracking infinito. | Parser de Logs/Búsqueda de archivos | Uso CPU = 100%, proceso colgado (ReDoS) | min | Fatal | Limitar Backtracking / Usar RE2 de Google en lugar de `re` |
| PRM-066 | Contención Falsa de Hilos (GIL Lockout) | Código CPU-bound ejecuta bajo `threading`, bloqueando el Global Interpreter Lock e impidiendo el Event Loop. | Operaciones de NLP locales / Criptografía masiva | Latencia disparada en I/O | seg | Crítica | Desplazar tarea a `ProcessPoolExecutor` |
| PRM-067 | Desfase de Configuración C-Ext | Instalar `cryptography` precompilado en Mac Intel (x86_64) e intentar ejecutar en Mac Silicon (arm64). | Rosetta 2 activo erróneamente en CLI | `Mach-O, but wrong architecture` | ms | Fatal | Entorno Python estricto arm64 (`arch -arm64`) |
| PRM-068 | Interbloqueo Asíncrono (Deadlock) | Tarea A espera a Tarea B, y Tarea B espera un recurso bloqueado por Tarea A. | Uso de `asyncio.Lock` desordenado | Event Loop congelado, 0% CPU, no responde | infinito | Fatal | Ordenación topológica estricta para adquisición de Locks |
| PRM-069 | Fuga Lateral de Memoria (C-Extensions) | Pandas/Numpy asignan memoria en C que el recolector de basura de Python no ve. | Carga de DataFrames de 10GB continuos | OOM del Kernel, cierre silencioso (Killed) | horas | Grave | Liberación explícita `del data` + recolección `gc.collect()` forzosa |
| PRM-070 | Hambre del Subagente Root (Root Starvation) | Subagentes colapsan los núcleos, el orquestador no puede despachar mensajes nuevos. | Carga > Múltiplos del número de núcleos lógicos | MOSKV-1 deja de responder a la terminal | min | Crítica | Reservar (Pin) un core lógico exclusivamente para el Gateway principal |
| PRM-071 | Saturación del Logger Binario (Binary Log Spit) | Intentar loguear el contenido binario de una imagen WebP de 10MB en la terminal. | Payload raw pasado a un `print()` / logger | Terminal inundada con caracteres ascii ininteligibles y pitidos (`\a`) | seg | Moderada | Filtro `is_binary` en la clase Logger |
| PRM-072 | Inyección de Falso Delimitador YAML | El modelo emite ````yaml` dos veces, o incluye saltos confusos. | Truncamiento de output LLM | El parser BFT de CORTEX rechaza el output por sintaxis YAML inválida | ms | Grave | Parser resiliente que busca el primer bloque válido y lo aísla |
| PRM-073 | Retraso Exponencial de Watchdog | Mac OS `fsevents` crashea y el fallback usa Polling que quema CPU. | Límite de `fseventsd` superado | Kernel de polling saturado (CPU 30%) | min | Moderada | Reinicio automático de watchdog de sistema nativo |
| PRM-074 | Caída Silenciosa de Sidecar (Redis/Neo4j) | El contenedor del servicio base muere, el sistema en Python no captura la conexión rota rápido. | Daemon kill de Docker/Podman | Timeouts de 30s en cada query al DB | seg | Crítica | Healthchecks rápidos (`PING`) en cada transacción (Pool test) |
| PRM-075 | Saturación de Socket MCP | La comunicación vía MCP a una herramienta local satura la red de loopback. | Tráfico > 1GB/s por localhost | Latencias inter-proceso erráticas | ms | Grave | Serialización Binaria (MessagePack) vs JSON crudo |
| PRM-076 | Invocación Cíclica de Skills | La Skill A llama a Skill B, y B llama a A por reglas solapadas en `.gemini/config`. | Falta de control de árbol jerárquico | Bucle infinito de `view_file(SKILL.md)` | min | Fatal | Rastreador de Grafo de Invocación (Call Stack de Skills) acotado |
| PRM-077 | Falla Criptográfica de Archivo Cero | Intentar hacer Hash `SHA-256` de un archivo que se ha creado pero sigue vacío (`0 bytes`). | Flush asíncrono pendiente en disco | Hash erróneo persistido en DB (Validación cruzada falla siempre) | ms | Moderada | Forzar `os.fsync` previo a lecturas criptográficas |
| PRM-078 | Colapso de Coseno de Alta Dimensión | Calcular similitud en embeddings con dimensiones 4096 sin normalización previa a unidad. | Desbordamiento numérico (Overflow) | Valores de similitud absurdos (Ej: >1.0 o NaN) | ms | Fatal | L2 Normalization estricta pre-cálculo |
| PRM-079 | Agotamiento del Presupuesto (Token Spend Cap) | La cuota límite de USD$ por hora definida en `billing_guard` se excede. | Agente rebelde en mitigación | Aborto de ejecución global con error "Cuota Termodinámica Vaciada" | instantáneo | Crítica | Reinicio BFT manual requerido (Operador Biológico) |
| PRM-080 | Error Temporal de Redirección (301 Infinite Loop) | Scraping o RAG Crawler se queda atrapado en dos páginas que se redirigen mutamente. | Archivos estáticos mal configurados | Tráfico bloqueado | seg | Grave | `max_redirects=3` estricto en el Web Crawler |
| PRM-081 | Truncamiento Silencioso por OS Pager | Ejecutar comando bash y el OS lanza `less` / `more`, bloqueando el pipe. | Salidas > Altura de terminal simulada | Tarea asíncrona no termina jamás, esperando un Enter (`\n`) ficticio | infinito | Fatal | Exportación `PAGER=cat` en cada sesión PTY |
| PRM-082 | Parálisis de Peticiones API (TLS Handshake Hang) | El handshake de HTTPS (TLS) se cuelga eternamente y el request no lanza `Timeout`. | Fallos oscuros del stack TCP del Host o del Proveedor (Cloudflare). | El subagente se bloquea indefinidamente sin lanzar error de Python. | horas | Crítica | Timeouts rígidos a nivel de Capa de Transporte (TCP) en `aiohttp`/`httpx`. |
