# MATRIZ 2: BATCH 17-20 (Autonomía Asíncrona - C5-REAL)

| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
| :--- | :--- | :--- | :--- | :--- | :--- |
| INV-152 | Soberanía Asíncrona (Sub-152) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-153 | Soberanía Asíncrona (Sub-153) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-154 | Soberanía Asíncrona (Sub-154) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-155 | Soberanía Asíncrona (Sub-155) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-156 | Soberanía Asíncrona (Sub-156) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-157 | Soberanía Asíncrona (Sub-157) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-158 | Soberanía Asíncrona (Sub-158) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-159 | Soberanía Asíncrona (Sub-159) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-160 | Soberanía Asíncrona (Sub-160) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-161 | Soberanía Asíncrona (Sub-161) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-162 | Soberanía Asíncrona (Sub-162) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-163 | Soberanía Asíncrona (Sub-163) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-164 | Soberanía Asíncrona (Sub-164) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-165 | Soberanía Asíncrona (Sub-165) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-166 | Soberanía Asíncrona (Sub-166) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-167 | Soberanía Asíncrona (Sub-167) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-168 | Soberanía Asíncrona (Sub-168) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-169 | Soberanía Asíncrona (Sub-169) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-170 | Soberanía Asíncrona (Sub-170) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-171 | Soberanía Asíncrona (Sub-171) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-172 | Soberanía Asíncrona (Sub-172) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-173 | Soberanía Asíncrona (Sub-173) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-174 | Soberanía Asíncrona (Sub-174) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-175 | Soberanía Asíncrona (Sub-175) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-176 | Soberanía Asíncrona (Sub-176) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-177 | Soberanía Asíncrona (Sub-177) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-178 | Soberanía Asíncrona (Sub-178) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-179 | Soberanía Asíncrona (Sub-179) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-180 | Soberanía Asíncrona (Sub-180) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-181 | Soberanía Asíncrona (Sub-181) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-182 | Soberanía Asíncrona (Sub-182) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-183 | Soberanía Asíncrona (Sub-183) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-184 | Soberanía Asíncrona (Sub-184) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-185 | Soberanía Asíncrona (Sub-185) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-186 | Soberanía Asíncrona (Sub-186) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-187 | Soberanía Asíncrona (Sub-187) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-188 | Soberanía Asíncrona (Sub-188) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-189 | Soberanía Asíncrona (Sub-189) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-190 | Soberanía Asíncrona (Sub-190) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-191 | Soberanía Asíncrona (Sub-191) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-192 | Soberanía Asíncrona (Sub-192) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-193 | Soberanía Asíncrona (Sub-193) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-194 | Soberanía Asíncrona (Sub-194) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-195 | Soberanía Asíncrona (Sub-195) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-196 | Soberanía Asíncrona (Sub-196) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-197 | Soberanía Asíncrona (Sub-197) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-198 | Soberanía Asíncrona (Sub-198) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-199 | Soberanía Asíncrona (Sub-199) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |
| INV-200 | Soberanía Asíncrona (Sub-200) | El Kernel asume el control absoluto del DAG de promesas y el Event Loop. | Evitar que el Operador micro-gestione esperas (awaits) o bloqueos manuales; la máquina auto-resuelve. | Intervención humana en un bucle asíncrono o deadlock de I/O. | Tasa de interrupciones asíncronas requeridas al Operador (debe ser 0). |