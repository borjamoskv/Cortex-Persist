services:
  - type: web
    name: cortex-persist-api
    env: python
    buildCommand: "pip install -e .[api]"
    startCommand: "uvicorn x100_cortex_server:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.0"
      - key: PORT
        value: "10000"
